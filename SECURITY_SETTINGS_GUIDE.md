# 安全設定功能使用指南

## 功能概述

本系統提供了兩層安全保護機制：
1. **IP 白名單** - 限制特定 IP 才能登入（由 Super Admin 管理）
2. **雙因素認證 (2FA)** - 使用 Google Authenticator 提供額外保護（店家可自行設定）

---

## 角色與權限

### Super Admin（超級管理員）
- **控制範圍**：所有店家的安全設定
- **功能**：
  - 啟用/停用店家的 IP 白名單功能
  - 設定/修改店家的 IP 白名單
  - 啟用/停用店家的 2FA 功能
  - **臨時關閉**店家的 2FA（24小時後自動恢復）
  - 立即恢復被臨時關閉的 2FA

### 店家（Store Owner）
- **控制範圍**：自己的 2FA 設定
- **功能**：
  - 啟用自己的 2FA
  - 掃描 QR Code 設定 Google Authenticator
  - 輸入驗證碼確認 2FA 設定
  - 停用自己的 2FA

### 權限說明
- ❌ **店家無法看到或設定 IP 白名單**（這是 Super Admin 專屬功能）
- ✅ **店家可以自主管理 2FA**（前提是 Super Admin 已啟用）

---

## 完整使用流程

### 一、Super Admin 設定店家安全功能

#### 1.1 登入 Super Admin 帳號

```
Email: admin@592meal.com
Password: password
```

#### 1.2 進入店家管理
- **方法 A**：側邊欄 → 「👥 店家管理」
- **方法 B**：直接訪問 `/admin/users`

#### 1.3 編輯店家資料
1. 在店家列表找到目標店家
2. 點擊該列的「✏️ 編輯」按鈕

#### 1.4 設定安全功能

在「**安全設定**」區塊中：

##### 選項 1：啟用 IP 白名單
```
☑️ 啟用 IP 白名單
   ℹ️ 啟用後，只有白名單內的 IP 可以登入

📋 IP 白名單
   192.168.1.100
   192.168.1.101
   (可輸入多個 IP，每行一個)
```

##### 選項 2：啟用 2FA
```
☑️ 啟用 2FA (雙因素認證)
   ℹ️ 啟用後，登入時需要輸入 Google Authenticator 驗證碼

📊 2FA 狀態
   顯示：❌ 未設定 / ⚠️ 已設定但未確認 / ✅ 已確認
```

#### 1.5 儲存設定
點擊「儲存」按鈕

---

### 二、店家設定 2FA（雙因素認證）

#### 前提條件
- Super Admin 已為該店家「啟用 2FA」功能

#### 2.1 登入店家帳號
使用店家自己的帳號登入系統

#### 2.2 進入安全設定頁面
- **方法 A**：側邊欄 → 「🛡️ 安全設定」
- **方法 B**：直接訪問 `/admin/security-settings`

#### 2.3 啟用 2FA（第一次設定）

**步驟**：
1. 點擊右上角「**啟用 2FA**」按鈕
2. 系統會生成 QR Code

#### 2.4 掃描 QR Code

**使用 Google Authenticator App**：

1. 在手機上下載並開啟 Google Authenticator
   - iOS: App Store 搜尋「Google Authenticator」
   - Android: Google Play 搜尋「Google Authenticator」

2. 點擊 App 中的「+」按鈕

3. 選擇「掃描 QR Code」

4. 掃描螢幕上顯示的 QR Code

5. App 會顯示 6 位數驗證碼（每 30 秒更新一次）

#### 2.5 確認設定

1. 在「**驗證碼**」欄位輸入 App 顯示的 6 位數驗證碼

2. 點擊右上角「**確認 2FA**」按鈕

3. 如果驗證碼正確：
   - ✅ 顯示「2FA 確認成功」
   - 狀態變更為「✅ 已確認」

4. 如果驗證碼錯誤：
   - ❌ 顯示「驗證碼錯誤」
   - 請檢查驗證碼並重試

#### 2.6 停用 2FA（可選）

如果需要停用 2FA：
1. 進入「安全設定」頁面
2. 點擊右上角「**停用 2FA**」按鈕
3. 確認停用

---

### 三、Super Admin 臨時關閉 2FA（緊急情況）

#### 使用情境
- 店家遺失手機
- 店家更換手機
- 店家無法取得驗證碼

#### 操作步驟

##### 3.1 臨時關閉 2FA

1. 進入「店家管理」→ 編輯該店家

2. 在編輯頁面右上角，點擊「**臨時關閉 2FA (24小時)**」按鈕

3. 確認操作

4. 系統顯示：
   ```
   ⚠️ 2FA 已臨時關閉
   將於 2025-10-10 14:30 自動恢復
   ```

5. 在「2FA 狀態」欄位會顯示：
   ```
   🔒 臨時關閉中 (還有 23 小時後自動恢復)
   關閉時間: 2025-10-09 14:30
   恢復時間: 2025-10-10 14:30
   ```

##### 3.2 立即恢復 2FA

如果店家已解決問題（例如：重新設定 Google Authenticator），Super Admin 可以：

1. 進入「店家管理」→ 編輯該店家

2. 在編輯頁面右上角，點擊「**立即恢復 2FA**」按鈕

3. 確認操作

4. 系統顯示：
   ```
   ✅ 2FA 已恢復
   此店家的雙因素認證已恢復正常
   ```

---

### 四、自動恢復機制

#### 機制說明
- 當 Super Admin 臨時關閉 2FA 後，系統會記錄關閉時間
- **24 小時後**，系統會自動恢復 2FA
- 或者**店家重新設定 2FA** 時，也會立即恢復

#### 恢復方式

**方式 1：自動恢復（推薦）**
- 系統每小時執行一次檢查
- 自動恢復超過 24 小時的臨時關閉

**方式 2：店家重新設定**
- 店家進入「安全設定」頁面
- 重新啟用並確認 2FA
- 系統會自動清除臨時關閉狀態

**方式 3：Super Admin 手動恢復**
- Super Admin 在編輯頁面
- 點擊「立即恢復 2FA」按鈕

---

## 狀態顯示說明

### 2FA 狀態（在店家管理中顯示）

| 顯示 | 意思 | 店家應執行的動作 |
|------|------|-----------------|
| ❌ 未設定 | Super Admin 未啟用 2FA | 等待 Admin 啟用 |
| 📱 可以設定 | 已啟用但店家還沒掃描 | 進入「安全設定」掃描 QR Code |
| ⚠️ 已設定但未確認 | 已掃描但未輸入驗證碼 | 輸入驗證碼確認 |
| ✅ 已確認 | 完全設定完成 | 可正常使用 2FA 登入 |
| 🔒 臨時關閉中 | Super Admin 已臨時關閉 | 可登入，24小時後恢復 |

---

## 常見問題 (FAQ)

### Q1: 為什麼店家看不到 IP 白名單設定？
**A**: IP 白名單是 Super Admin 專屬功能，店家無權查看或修改。這是為了確保 IP 控制由管理員統一管理。

### Q2: 店家遺失手機後怎麼辦？
**A**:
1. 店家聯繫 Super Admin
2. Super Admin 臨時關閉該店家的 2FA（24小時）
3. 店家可在此期間登入並重新設定 2FA
4. 店家重新設定完成後，臨時關閉狀態會自動解除

### Q3: 24小時自動恢復會不會太長？
**A**:
- 這個時間是為了給店家足夠時間處理問題（例如：等手機維修、購買新手機）
- Super Admin 可以隨時手動「立即恢復」
- 店家重新設定完成後會立即恢復，不需要等 24 小時

### Q4: 如果店家在臨時關閉期間重新設定 2FA，會怎樣？
**A**: 系統會自動清除臨時關閉狀態，2FA 立即生效。

### Q5: Super Admin 可以看到店家的 2FA 驗證碼嗎？
**A**: **不能**。2FA secret 是加密儲存的，只有店家本人可以看到 QR Code 並設定。

### Q6: IP 白名單和 2FA 可以同時使用嗎？
**A**: **可以**。Super Admin 可以為店家同時啟用 IP 白名單和 2FA，提供雙重保護。

### Q7: 如何啟動自動恢復的排程任務？
**A**: 在生產環境中，需要設定 cron job：
```bash
* * * * * cd /path/to/project && php artisan schedule:run >> /dev/null 2>&1
```
這會每分鐘檢查是否有需要執行的排程任務（包括每小時的 2FA 恢復檢查）。

---

## 技術說明

### 資料庫欄位

#### users 表

| 欄位 | 類型 | 說明 |
|------|------|------|
| `ip_whitelist_enabled` | boolean | IP 白名單是否啟用 |
| `ip_whitelist` | json | IP 白名單列表 |
| `two_factor_enabled` | boolean | 2FA 是否啟用 |
| `two_factor_secret` | text | 2FA 密鑰（加密） |
| `two_factor_recovery_codes` | text | 2FA 恢復碼（加密） |
| `two_factor_confirmed_at` | timestamp | 2FA 確認時間 |
| `two_factor_temp_disabled_at` | timestamp | 2FA 臨時關閉時間 |

### 重要文件

| 文件路徑 | 說明 |
|---------|------|
| `app/Models/User.php` | User Model（包含 2FA 和 IP 白名單方法） |
| `app/Filament/Pages/SecuritySettings.php` | 店家安全設定頁面 |
| `app/Filament/Resources/UserResource.php` | Super Admin 店家管理頁面 |
| `app/Console/Commands/RestoreExpiredTwoFactorDisable.php` | 自動恢復 command |
| `app/Http/Middleware/CheckIpWhitelist.php` | IP 白名單檢查 middleware |
| `database/migrations/*_add_security_fields_to_users_table.php` | 安全欄位 migration |
| `database/migrations/*_add_two_factor_temp_disabled_to_users_table.php` | 臨時關閉欄位 migration |

### CLI 命令

#### 手動檢查和恢復過期的臨時關閉
```bash
php artisan two-factor:restore-expired
```

#### 查看排程任務列表
```bash
php artisan schedule:list
```

#### 測試排程任務（不實際執行）
```bash
php artisan schedule:work
```

---

## 安全建議

### 對 Super Admin
1. ✅ 定期檢查店家的安全設定
2. ✅ 為重要店家啟用 IP 白名單 + 2FA 雙重保護
3. ✅ 臨時關閉 2FA 時，應通知店家盡快重新設定
4. ⚠️ 不要濫用「臨時關閉」功能
5. ⚠️ 保管好自己的 Super Admin 帳密

### 對店家
1. ✅ 建議啟用 2FA 提升帳號安全性
2. ✅ 保管好手機和 Google Authenticator
3. ✅ 備份 2FA 恢復碼（如果有提供）
4. ⚠️ 不要將驗證碼分享給他人
5. ⚠️ 更換手機前請先停用 2FA，再於新手機重新設定

---

## 更新日誌

### v2.0 (2025-10-09)
- ✅ 重新設計安全設定頁面
- ✅ 移除店家對 IP 白名單的訪問權限
- ✅ 添加 2FA 確認輸入框和驗證流程
- ✅ 實作 Super Admin 臨時關閉 2FA 功能
- ✅ 實作 24 小時自動恢復機制
- ✅ 添加「立即恢復」功能
- ✅ 優化 2FA 狀態顯示

### v1.0 (2025-10-09)
- ✅ 初始版本
- ✅ IP 白名單功能
- ✅ 2FA 基本功能
- ✅ Super Admin 管理介面

---

## 聯絡支援

如有問題或建議，請聯繫系統管理員。

---

**文檔版本**: v2.0
**最後更新**: 2025-10-09
**作者**: Claude Code
**Laravel 版本**: 12.32.5
**PHP 版本**: 8.4.12
**Filament 版本**: 4.1.6
